% ============================================================
%  Section 4.5 – Tests du χ²
% ============================================================
%
%  χ² de conformité (§ 3.4.2 du cours) :
%      H0 : la distribution de X suit la loi de référence L
%      Effectifs théoriques : ni = n × P(X ∈ classei | loi L)
%      Critère : χ² = Σ (oi − ni)² / ni
%      ddl = k − 1 ; seuil dans table Annexe 5.3
%      Conditions : n≥50, ni≥5
%
%  χ² d'indépendance (§ 3.4.2 du cours) :
%      H0 : X et Y sont indépendantes
%      Effectifs théoriques : ni,j = oi,• × o•,j / n
%      Critère : χ² = ΣΣ (oi,j − ni,j)² / ni,j
%      ddl = (k1−1)(k2−1) ; seuil dans table Annexe 5.3
%      Conditions : n≥50, ni,j≥5
%
%  Présenter les tableaux de contingence observé ET théorique côte à côte.
%  Si conditions non réunies : le mentionner et présenter le test quand même.
%
% ============================================================

\subsection{Tests du $\chi^2$}

% TODO : tableaux de contingence + critère + conclusion
